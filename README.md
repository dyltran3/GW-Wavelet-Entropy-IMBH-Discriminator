🌌 Decoding the Mass Gap: A Wavelet-Entropy Approach to Distinguish IMBH Signals from LIGO Glitches📝 1. TÓM TẮT DỰ ÁN (PROJECT SUMMARY)Dự án này là một nghiên cứu định lượng nhằm giải quyết bài toán nhập nhằng tín hiệu (signal ambiguity) trong thiên văn học Sóng hấp dẫn (Gravitational Wave - GW). Cụ thể, chúng tôi tập trung vào việc phân biệt Tín hiệu sáp nhập Lỗ đen Trung gian (IMBH) khối lượng lớn (nằm trong "Vùng cấm" lý thuyết) với các loại Nhiễu xung (Blip Glitches) của máy dò LIGO/Virgo.Phương pháp cốt lõi là sử dụng Biến đổi Wavelet Liên tục (CWT) để tối ưu hóa độ phân giải thời gian-tần số, sau đó áp dụng chỉ số Quadratic Entropy ($H_Q$) để định lượng sự khác biệt về cấu trúc vật lý giữa tín hiệu thật (có trật tự) và nhiễu (hỗn loạn).🚀 2. MỤC TIÊU VÀ TÍNH MỚI (NOVELTY)Đề tài này vượt qua các phân tích tái tạo (reproduction) cơ bản bằng cách tập trung vào:Kiểm chứng O4 Mới nhất: Áp dụng phương pháp phân loại mới lên các sự kiện ứng viên IMBH trong dữ liệu LIGO O4 Run (thay vì dữ liệu O1/O2/O3 cũ).Mô hình Diễn giải (Interpretable Model): Thay vì sử dụng "hộp đen" AI, chúng tôi xây dựng một mô hình dựa trên vật lý diễn giải (Entropy là thước đo độ phức tạp/trật tự vật lý) để tạo ra một chỉ số phân biệt minh bạch và đáng tin cậy.🛠️ 3. PHƯƠNG PHÁP LUẬN CỐT LÕI (CORE METHODOLOGY)3.1. Khoa học Xử lý Tín hiệuCông cụ Phân tích: Biến đổi Wavelet Liên tục (CWT) với hàm mẹ Morlet. Wavelet được chọn vì nó khắc phục được hạn chế về độ phân giải thời gian-tần số của Biến đổi Fourier ngắn hạn (STFT) đối với các tín hiệu ngắn (burst-like signals) như IMBH và Glitch.Chỉ số Phân biệt: Quadratic Entropy ($H_Q$). QE định lượng mức độ tập trung năng lượng trong biểu đồ Wavelet Scalogram.Giả thuyết: Tín hiệu IMBH có cấu trúc sóng vật lý $\rightarrow$ $H_Q$ thấp hơn và tập trung hơn. Nhiễu Blip có cấu trúc hỗn loạn $\rightarrow$ $H_Q$ cao hơn và phân tán hơn.Công thức:$$H_Q = 1 - \sum_{i} p_i^2 \quad \text{(với } p_i \text{ là xác suất năng lượng tại điểm } i \text{ trên phổ Wavelet)}$$3.2. Đánh giá Hiệu suất Thống kêChỉ số Quyết định: Ngưỡng Entropy ($H_{Q, \text{ngưỡng}}$) sẽ được tối ưu bằng cách sử dụng đường cong ROC (Receiver Operating Characteristic).Tối ưu hóa: Tìm điểm tối ưu trên đường cong ROC (sử dụng chỉ số Youden's J statistic) để cân bằng tỷ lệ Phát hiện đúng (True Positive Rate) và tỷ lệ Báo động giả (False Positive Rate).🎯 4. CÁC CÂU HỎI NGHIÊN CỨU CỐT LÕICác Module của dự án được thiết kế để trả lời các câu hỏi sau:Hiệu suất CWT: CWT/Entropy có mang lại hiệu suất phân loại tốt hơn đáng kể (thể hiện qua đường cong ROC) so với các chỉ số dựa trên FFT/Q-transform truyền thống trong việc phân biệt IMBH và Glitch Blip không?Định lượng Phân bố: Phân bố Entropy của nhiễu Glitch Blip có ổn định về mặt thống kê theo thời gian (giữa O3 và O4) hay không, và độ phân tán Entropy của nhiễu có cao hơn đáng kể so với Entropy của tín hiệu IMBH giả lập không?Kiểm chứng Dữ liệu O4: Chỉ số $H_Q$ được tối ưu trên dữ liệu cũ có thể được áp dụng thành công để kiểm chứng các sự kiện ứng viên IMBH mới nhất từ O4 Run hay không, và đưa ra một ngưỡng tin cậy để loại bỏ các trường hợp dương tính giả?⚙️ 5. HƯỚNG DẪN CÀI ĐẶT & SỬ DỤNG5.1. Yêu cầu Hệ thống (Prerequisites)Python 3.9+ (Khuyến nghị sử dụng Anaconda/Miniconda).Truy cập Internet (để tải dữ liệu GWOSC).5.2. Cài đặt Môi trường (Installation)Tất cả các thư viện cần thiết đều có sẵn qua pip:Bash# Tạo môi trường ảo
conda create -n gw_env python=3.9
conda activate gw_env

# Cài đặt các thư viện lõi
pip install numpy scipy matplotlib pandas

# Cài đặt các thư viện GW chuyên dụng (PyCBC, GWpy, PyWavelets)
pip install gwpy pycbc pywt
5.3. Quy trình làm việc (Workflow)Toàn bộ dự án được tổ chức theo các thư mục Module tương ứng với các giai đoạn phân tích:Thư mụcNội dungModule Trọng tâm01_simulation/Code tạo bộ dữ liệu IMBH giả lập (sử dụng PyCBC).Module 202_data_mining/Code tải và phân loại Blip Glitches (sử dụng GWOSC API).Module 303_cwt_engine/Code triển khai hàm CWT (Morlet) và hàm tính Quadratic Entropy ($H_Q$).Module 104_statistical_analysis/Notebooks thực hiện phân tích ROC và xác định ngưỡng phân loại tối ưu ($H_{Q, \text{ngưỡng}}$).Module 405_o4_validation/Script áp dụng thuật toán lên các ứng viên GW O4 mới.Kiểm chứng cuối cùng📞 LIÊN HỆ VÀ GIẤY PHÉPTác giả: [Tên của bạn]Giấy phép (License): MIT (Khuyến nghị cho các dự án mã nguồn mở)(Nếu bạn muốn đóng góp hoặc có câu hỏi về phương pháp luận, vui lòng liên hệ qua GitHub Issues.)
